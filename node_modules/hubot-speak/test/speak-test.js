// Generated by CoffeeScript 1.8.0
var chai, expect, sinon;

chai = require('chai');

sinon = require('sinon');

chai.use(require('sinon-chai'));

expect = chai.expect;

describe('speak', function() {
  beforeEach(function() {
    this.robot = {
      respond: sinon.spy(),
      hear: sinon.spy()
    };
    return require('../src/speak')(this.robot);
  });
  return it('registers a respond listener', function() {
    expect(this.robot.hear).to.have.been.calledWith(/say something ([A-Z]||[a-z]*)?/i);
    return expect(this.robot.hear).to.have.been.calledWith(/say something stupid/i);
  });
});
