chai = require "chai"
sinon = require "sinon"
chai.use require "sinon-chai"

expect = chai.expect

describe "remind", ->
    before ->
        @robot =
            brain:
                on: sinon.spy()
            respond: sinon.spy()
            hear: sinon.spy()

        require("../src/remind")(@robot)

    it "registers a respond listener", ->
        expect(@robot.respond).to.have.been.calledWith(/recuerdame (.+)/i)
        expect(@robot.respond).to.have.been.calledWith(/cancela recordatorio/i)
        expect(@robot.respond).to.have.been.calledWith(/recuerdame ahora/i)
        expect(@robot.respond).to.have.been.calledWith(/limpia recordatorios/i)

    it "registers a hear listener", ->
        expect(@robot.hear).to.have.been.calledWith(/(.+)/i)

    it "registers a brain event listener", ->
        expect(@robot.brain.on).to.have.been.calledWith("loaded")
